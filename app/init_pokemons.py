# 모든 포켓몬 cacheing
import pokebase,requests

# def init_pokemons():
    # num_pokemons = requests.get('https://pokeapi.co/api/v2/pokemon').json()['count']
    # for i in range(0, int(num_pokemons)):  # cacheing
    #     pokebase.pokemon(i)
